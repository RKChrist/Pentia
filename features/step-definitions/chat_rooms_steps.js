const { Given, Then, When } = require('@cucumber/cucumber');

Given('I am on the chat rooms screen', function () {
  // Mock being on the chat rooms screen
});

Then('a list of chat rooms should be displayed with name and description', function () {
  // Verify that the list of chat rooms is displayed correctly
});

When('I press a chat room', function () {
  // Mock the action of pressing a chat room
});

Then('I should be taken to the send and receive messages screen', function () {
  // Verify navigation to the send and receive messages screen
});
